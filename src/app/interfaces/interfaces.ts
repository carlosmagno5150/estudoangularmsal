export interface UserInfo {
  tenantId: string;
  clientId: string;
  email: string;
  Token: string;
}

export interface WeatherInfo{
  date: string;
  summary: string;
  temperatureC: number;
  temperatureF: number;
}
